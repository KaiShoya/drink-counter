# [T-0030] 加算/減算操作に Undo トーストを追加

**ID**: T-0030  
**作成日**: 2026-05-09  
**優先度**: Medium  
**種別**: enhancement  
**ステータス**: done  
**担当バージョン**: v1.22  
**関連 Issue**: -

---

## Goal

トップ画面の誤タップを短時間で取り消せるようにし、入力ミス復帰を簡単にする。

## Scope（対象ファイル・範囲）

- `app/pages/(protected)/index.vue`
- `app/stores/pages/index.ts`（または関連 store）
- `app/utils/toast.ts`（必要に応じて）

## Out of Scope

- 全画面へのUndo共通化
- サーバー側イベントソーシング導入

## Dependencies

- T-0029 と合わせて検討推奨

## Plan

1. 操作ログの最小履歴（直近1件）を保持する。
2. 加算/減算後に Undo 可能トーストを表示する。
3. タイムアウト後は取り消し不可にする。

## Verification

- [x] Undo で直前操作が巻き戻る
- [x] 連続操作時に整合性が崩れない
- [x] タイムアウト後にUndoできない

## Next Action

完了。必要時のみUndo有効時間や表示文言を調整する。

## Notes

- 仕様変更により store テスト拡充が必要。

## Work Log

- 2026-05-11: T-0030 着手。トップ画面の加算/減算フローと `toast.ts` の既存APIを確認。
- 2026-05-11: `index` ページに「直近1件のみ」Undoトークン管理を追加し、加算/減算操作に Undo トーストを接続。
- 2026-05-11: `plusCheck` と警告モーダル経由の加算にも `plusWithUndo` を適用し、操作経路を統一。
- 2026-05-11: `showUndoToast` を `app/utils/toast.ts` に追加し、`toast.spec.ts` にテストケースを追加。
- 2026-05-11: `index.undo_*` i18n キーを `locales.ts` / `i18n/locales/{ja,en}.yaml` に追加。
- 2026-05-11: エラー確認（0件）と全体テスト（44 passed / 0 failed）を実施し、status を review に更新。
- 2026-05-11: 回帰修正。`showUndoToast` の `textContent` に HTML 文字列を入れていたためタグが生表示される不具合を、プレーンテキスト表示に変更して解消。
- 2026-05-11: 追加調整。Undo 通知に `ラベル名 / 飲み物名` を表示し、減算時に通知が欠落する比較ロジックを修正。Undo トーストは `is-primary` に変更し、閉じる `x` を非表示化して「元に戻す」ボタンとの重なりを解消。

## Closure Notes

- 2026-05-11: T-0030 を done に変更。タスクファイルを `items/done/` へ移動してクローズ。
