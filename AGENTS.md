# drink-counter 作業ガイド

## 正本と参照順

- プロダクト、要件、設計、意思決定、開発・QAの資料は `docs/` を正本とする。
- 作業を始める前に、対象に対応する現行の `docs/` を確認する。仕様が未記載または矛盾する場合は推測で確定しない。
- `docs/99_archive/` は履歴保存用であり、現行仕様や実装判断の根拠にしない。
- コード・仕様・運用手順を変更したときは、同じ変更で関連する `docs/` を更新する。

## ドキュメント運用

- `01_product/`：目的、利用者、価値、用語
- `02_requirements/`：機能・非機能要件、受け入れ条件
- `03_spec/`：アーキテクチャ、データモデル、API、画面・実装仕様
- `04_decisions/`：重要な判断の背景、決定、理由、採用しなかった案
- `05_discussions/`：未解決事項、調査、検討中の選択肢
- `06_execution/`：実装計画、タスク、QA、リリース・運用手順

新規資料は該当する番号付きディレクトリに置き、`docs/README.md` の主要資料一覧も必要に応じて更新する。解決済みの検討事項は決定記録または現行仕様へ反映する。

## 実装・品質

- TypeScript の型安全性を保ち、既存の Nuxt 自動 import を重複 import しない。
- 文言は i18n ロケールで管理し、UIへ直接埋め込まない。
- データアクセスは Repository を経由し、副作用と UI ロジックを分離する。
- DB変更は新しい Supabase migration で管理し、適用済み migration は編集しない。
- 変更内容に応じて型検査・関連テスト・ビルドを実行し、実行できない検証は理由を報告する。

## 変更管理

- 利用者の既存変更を戻したり、対象外のファイルを変更したりしない。
- コミットは Conventional Commits を使い、実装・対応テスト・必要なドキュメントを同じ論理単位に含める。
- コミット前に `git status`、対象差分、`git diff --check` を確認する。
