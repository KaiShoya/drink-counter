# [T-0001] Warning モーダルの × ボタンが機能しない

**ID**: T-0001  
**作成日**: 2026-05-06  
**優先度**: High  
**種別**: bug  
**ステータス**: done  
**担当バージョン**: v1.20  
**関連 Issue**: -

---

## Goal

`CommonModalMoleculesWarning` の閉じるボタン（×）をクリックしてもモーダルが閉じない問題を修正する。

## Scope（対象ファイル・範囲）

- `app/components/common/modal/molecules/Warning.vue`

## Out of Scope

- Danger モーダルなど他のモーダルコンポーネントの調査（別タスクで対応）

## Plan

1. `Warning.vue` の `<button class="delete" aria-label="close" />` に `@click="cancel()"` を追加する

## Verification

- [x] × ボタンをクリックするとモーダルが閉じる
- [x] `cancel` prop が呼び出される（`Danger.vue` と同パターンで実装確認）

## Work Log

- 2026-05-07: `Warning.vue` の × ボタンに `@click="cancel()"` を追加（`Danger.vue` と同パターン）
- `Danger.vue` は既に `@click="cancel()"` が実装済みであることを確認

## Notes

- `Danger.vue` にも同様の問題がないか確認すること
- 参照: `app/pages/(protected)/index.vue` の `CommonModalMoleculesWarning` 呼び出し
